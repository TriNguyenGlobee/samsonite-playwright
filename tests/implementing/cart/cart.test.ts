import { test, expect } from "../../../src/fixtures/test-fixture";
import { CartPage } from "../../../src/pages/implementing/cart/cart.page";
import { Config } from "../../../config/env.config";
import { step } from "allure-js-commons";
import { getRandomArrayElement } from "../../../utils/helpers";

test.describe("Remove from cart", () => {
////a[normalize-space(text())="PARALUX HS"]//ancestor::div[contains(@class,"product-tile ")]//button[contains(text(),"Add to Cart")]
//div[@class="tile-group-left"][.//a[text()="マイルス"] and .//a[text()="バックパック M"]]//ancestor::div[contains(@class,"product-tile ")]//button[contains(text(),"カートに入れる")]
});
